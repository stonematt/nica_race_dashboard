node_modules/
.next/
out/
build/
coverage/

# PGlite's on-disk database — local derived state, never committed
.pglite/

# Fetched payloads carry minors' names. The corpus lives outside the tree
# (see docs/fixtures.md); this is belt-and-braces if anyone points it here.
data/
fixtures/

.env
.env.*
!.env.example

.DS_Store
*.log
.playwright-mcp/
tsconfig.tsbuildinfo

# Generated by Next on every build; regenerated, never hand-edited.
next-env.d.ts

# Agent worktrees and local session state.
.claude/worktrees/
